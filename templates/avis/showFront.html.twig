{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block content %}
    <h1 class="mb-4 text-3xl font-extrabold  text-white md:text-5xl lg:text-6xl">
                Détails des
                <span class="text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400">avis</span>
            </h1>

    <div style="width:50%">
        <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>IdAvis</th>
                <td>{{ avi.idAvis }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ avi.note }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.commentaire }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ avi.dateCreation ? avi.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    </div>
    <div class="navigation flex justify-center mb-8">
        {{ knp_pagination_render(pagination) }}
    </div>
    

    <a href="{{ path('app_avis_edit', {'idAvis': avi.idAvis}) }}">Modifier</a>

    {{ include('avis/_delete_form.html.twig') }}

    <a href="{{ path('app_avis_index') }}">Retour</a>
{% endblock %}
