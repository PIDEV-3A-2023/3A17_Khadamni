{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdStage</th>
                <td>{{ stage.idStage }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ stage.poste }}</td>
            </tr>
            <tr>
                <th>NomEntreprise</th>
                <td>{{ stage.nomEntreprise }}</td>
            </tr>
            <tr>
                <th>AdresseStage</th>
                <td>{{ stage.adresseStage }}</td>
            </tr>
            <tr>
                <th>DureeStage</th>
                <td>{{ stage.dureeStage }}</td>
            </tr>
            <tr>
                <th>TypeStage</th>
                <td>{{ stage.typeStage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stage_index') }}">back to list</a>

    <a href="{{ path('app_stage_edit', {'idStage': stage.idStage}) }}">edit</a>
        <a href="{{ path('app_stage_edit', {'idStage': stage.idStage}) }}">liste users</a>


    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
